export DOCKER_HOST=unix://${HOME}/.colima/default/docker.sock
export PATH="/opt/homebrew/opt/openjdk/bin:$PATH"

alias lw='ls -d [a-z]* 2>/dev/null'
alias la='ls -la'
alias ld='ls -ld'
alias lh='ls -lh'
